import { SectionHeader } from '../../components';
import { FormTrainingEvent } from '../../parts';

const CreateEvent = () => {
  return (
    <div className="p-5">
      <SectionHeader />
      <FormTrainingEvent />
    </div>
  );
};

export default CreateEvent;
